apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: muchtodo
  labels:
    app: muchtodo-backend
spec:
  selector:
    app: muchtodo-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30080
  type: NodePort